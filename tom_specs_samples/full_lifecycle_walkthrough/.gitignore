# Generated by running the walkthrough: the Phase-2 and Phase-3 renditions and
# the Phase-4 extracts, plus pub's own state.
build/
.dart_tool/

# Not committed, for the reason `author_solution_blueprint/.gitignore` gives:
# a sample resolves against the CURRENT published versions, and a committed
# lock file would silently pin a reader to the versions of the day it was
# written.
pubspec.lock
