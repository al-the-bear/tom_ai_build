// GENERATED by tom_specs_clitool SomCppEmitter (v0) — do not edit by hand.
// Typed object-model facade (header) over the generic tom_som_cpp_runtime document.

#ifndef TOM_SOM_CPP_V0_HPP
#define TOM_SOM_CPP_V0_HPP

#include <optional>
#include <string>

#include "tom_som_cpp_runtime.hpp"

namespace tom_som_v0 {

// Generated enum tokens for `Probability` values. The stored token is byte-
// identical across every language port, so documents stay cross-compatible.
struct Probability {
  static constexpr const char* low = "low";
  static constexpr const char* medium = "medium";
  static constexpr const char* high = "high";
  // Returns the token when it is a known Probability value, else std::nullopt.
  static std::optional<std::string> parse(const std::string& token);
  Probability() = delete;
};

// Forward declarations (classes reference each other by value).
class CurrentLandscapeAssessment;
class Risk;
class SolutionBlueprint;
class SolutionBlueprintOwnerForm;

class CurrentLandscapeAssessment : public som::SomNode {
 public:
  CurrentLandscapeAssessment(som::SpecDocument& doc, std::string path);
  std::string summary() const;
  void setSummary(const std::string& value);
};

class Risk : public som::SomNode {
 public:
  Risk(som::SpecDocument& doc, std::string path);
  std::string title() const;
  void setTitle(const std::string& value);
  std::optional<std::string> probability() const;
  void setProbability(const std::string& value);
};

// Root of a project definition document.
class SolutionBlueprint : public som::SomNode {
 public:
  // The model version this object model was generated against (§2.1).
  static constexpr const char* kModelVersion = "0.0";
  // Creates the typed facade at the document root and verifies the document's
  // authoring documentVersion is editable (§2.2); throws som::SomVersionError
  // when it is not.
  explicit SolutionBlueprint(som::SpecDocument& doc, const std::string& documentVersion = "");
  // Loads a `*.docspecs.yaml` document into the caller-owned `doc` and
  // returns the typed root with the document's authoring stamp already
  // applied (§ item 4) — one call for the former decode → loadJson →
  // thread-documentVersion sequence. `doc` is borrowed by the returned
  // root and must outlive it (RAII ownership model).
  static SolutionBlueprint loadYaml(som::SpecDocument& doc, const std::string& yaml);
  // Loads a `*.docspecs.yaml` document from the file at `path` into the
  // caller-owned `doc` — the file companion to loadYaml.
  static SolutionBlueprint loadFile(som::SpecDocument& doc, const std::string& path);
  // This object model's own model version (major.minor), per §2.1.
  std::string objectModelVersion() const;
  // Classifies a document's editability against this object model without
  // throwing — the non-throwing companion to the constructor check (§ item 8):
  // a read-only viewer can branch on the result instead of catching
  // som::SomVersionError. An empty documentVersion is the absent-stamp
  // sentinel and classifies as editable.
  static som::SomEditability editabilityFor(const std::string& documentVersion);
  // Why the system exists.
  std::string vision() const;
  void setVision(const std::string& value);
  SolutionBlueprintOwnerForm owner() const;
  // Returns the list view; element type: Risk (construct from item paths).
  som::SomList risks() const;
  // Returns the list view; element type: scalar (construct from item paths).
  som::SomList tags() const;
  CurrentLandscapeAssessment situation() const;
};

// Generated form facade for the `owner` @Form section.
class SolutionBlueprintOwnerForm : public som::SomNode {
 public:
  SolutionBlueprintOwnerForm(som::SpecDocument& doc, std::string path);
  std::string name() const;
  void setName(const std::string& value);
  std::string role() const;
  void setRole(const std::string& value);
};

/// Generated path constants for the `PD00` document root (item 11).
///
/// Each constant is the absolute generic path of a fixed section, for use with
/// the generic som::SpecDocument API instead of a raw string literal.
struct Pd00Paths {
  // `PD00/vision`
  static constexpr const char* vision = "PD00/vision";
  // `PD00/owner`
  static constexpr const char* owner = "PD00/owner";
  // `PD00/risks`
  static constexpr const char* risks = "PD00/risks";
  // `PD00/tags`
  static constexpr const char* tags = "PD00/tags";
  // `PD00/situation`
  static constexpr const char* situation = "PD00/situation";
  // `PD00/situation/summary`
  static constexpr const char* situationSummary = "PD00/situation/summary";
  Pd00Paths() = delete;
};

}  // namespace tom_som_v0

#endif  // TOM_SOM_CPP_V0_HPP
