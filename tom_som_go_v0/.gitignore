# Go build / test artifacts — regenerated by `go build` / `go test`. Only the
# generated module (tom_som_go_v0.go), go.mod, meta/, schemas/, the behavioural
# test and the hand-authored examples/ are tracked.
*.test
*.out
/vendor/
