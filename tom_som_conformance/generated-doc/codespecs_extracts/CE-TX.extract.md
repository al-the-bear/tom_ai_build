# CE-TX — Text

Generated by `spec_codespecs_extract` from the specification document rooted at `SBP`.
`CE-TX.extract.yaml` beside this file is the artifact of record; this is a view of it.

## Area

| | |
|---|---|
| CE code | `CE-TX` |
| Canonical id | `Text` |
| `@CodeSpecKind` value | `CodeSpecPart.text` |
| `Cs*` annotations | `CsText` |
| Built on | `TomText` (`tom_flutter_ui`) + `TomTextResourceProvider` (`tom_core_kernel`); message/i18n-key model `TomMessageKey` / `TomMessageKeyRegistry` (`tom_core_codespecs`) → **gap** (§5.8, §5.21) |
| Attribute surface | §5.8, §5.21 |
| Slice(s) | 1, 5 |
| Authoring step(s) | 2, 24 |
| Project(s) | `<app>_codespec_shared`, `<app>_codespec_client` |
| May cite | `CE-EL`, `CE-FM`, `CE-VA`, `CE-AC`, `CE-SC`, `CE-API`, `CE-ST`, `CE-NV`, `CE-AZ`, `CE-ER`, `CE-AU`, `CE-ID`, `CE-NT`, `CE-RP` |
| Catalogue source | codespecs_mapping.md §4.1 (parts catalogue) + §4.4.3 (emission slices) + §4.4.6 (authoring order) |

## Entries (43)

### 1. `content` — `MessageKeyEntry.content.key`

- headline: Work list title
- instanceId: `MSGKE-MKEY-1`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-1/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
screen.orders.title
```

### 2. `content` — `MessageKeyEntry.content.defaultCopy`

- headline: Work list title
- instanceId: `MSGKE-MKEY-1`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-1/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Orders
```

### 3. `content` — `MessageKeyEntry.content.description`

- headline: Work list title
- instanceId: `MSGKE-MKEY-1`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-1/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Page title of the order work list (SCR-01).
```

### 4. `content` — `MessageKeyEntry.content.key`

- headline: State filter label
- instanceId: `MSGKE-MKEY-2`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-2/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
screen.orders.filter.state
```

### 5. `content` — `MessageKeyEntry.content.defaultCopy`

- headline: State filter label
- instanceId: `MSGKE-MKEY-2`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-2/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
State
```

### 6. `content` — `MessageKeyEntry.content.description`

- headline: State filter label
- instanceId: `MSGKE-MKEY-2`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-2/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Label of the state selector on the work list filter bar.
```

### 7. `content` — `MessageKeyEntry.content.key`

- headline: State filter hint
- instanceId: `MSGKE-MKEY-3`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-3/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
screen.orders.filter.state.hint
```

### 8. `content` — `MessageKeyEntry.content.defaultCopy`

- headline: State filter hint
- instanceId: `MSGKE-MKEY-3`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-3/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Show only orders in the selected lifecycle state
```

### 9. `content` — `MessageKeyEntry.content.description`

- headline: State filter hint
- instanceId: `MSGKE-MKEY-3`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-3/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Helper text of the state selector.
```

### 10. `content` — `MessageKeyEntry.content.key`

- headline: Open order action
- instanceId: `MSGKE-MKEY-4`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-4/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
screen.orders.action.open
```

### 11. `content` — `MessageKeyEntry.content.defaultCopy`

- headline: Open order action
- instanceId: `MSGKE-MKEY-4`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-4/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Open
```

### 12. `content` — `MessageKeyEntry.content.description`

- headline: Open order action
- instanceId: `MSGKE-MKEY-4`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-4/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Row action that navigates from the work list to order detail.
```

### 13. `content` — `MessageKeyEntry.content.key`

- headline: Empty queue message
- instanceId: `MSGKE-MKEY-5`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-5/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
screen.orders.empty
```

### 14. `content` — `MessageKeyEntry.content.defaultCopy`

- headline: Empty queue message
- instanceId: `MSGKE-MKEY-5`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-5/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
No orders match the selected state filter
```

### 15. `content` — `MessageKeyEntry.content.description`

- headline: Empty queue message
- instanceId: `MSGKE-MKEY-5`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-5/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Shown on the work list when the filtered queue is empty, and as the status column's empty-data copy.
```

### 16. `content` — `MessageKeyEntry.content.key`

- headline: Clear filter action
- instanceId: `MSGKE-MKEY-6`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-6/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
screen.orders.empty.action.clear
```

### 17. `content` — `MessageKeyEntry.content.defaultCopy`

- headline: Clear filter action
- instanceId: `MSGKE-MKEY-6`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-6/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Clear filter
```

### 18. `content` — `MessageKeyEntry.content.description`

- headline: Clear filter action
- instanceId: `MSGKE-MKEY-6`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-6/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Recovery action offered on the empty work list.
```

### 19. `content` — `MessageKeyEntry.content.key`

- headline: Order detail title
- instanceId: `MSGKE-MKEY-7`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-7/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
screen.order.title
```

### 20. `content` — `MessageKeyEntry.content.defaultCopy`

- headline: Order detail title
- instanceId: `MSGKE-MKEY-7`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-7/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Order {orderId}
```

### 21. `content` — `MessageKeyEntry.content.placeholders`

- headline: Order detail title
- instanceId: `MSGKE-MKEY-7`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-7/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
orderId
```

### 22. `content` — `MessageKeyEntry.content.description`

- headline: Order detail title
- instanceId: `MSGKE-MKEY-7`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-7/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Page title of order detail (SCR-02).
```

### 23. `content` — `MessageKeyEntry.content.key`

- headline: Amend line action
- instanceId: `MSGKE-MKEY-8`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-8/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
screen.order.action.amend
```

### 24. `content` — `MessageKeyEntry.content.defaultCopy`

- headline: Amend line action
- instanceId: `MSGKE-MKEY-8`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-8/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Amend line
```

### 25. `content` — `MessageKeyEntry.content.description`

- headline: Amend line action
- instanceId: `MSGKE-MKEY-8`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-8/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Submit action on an order line.
```

### 26. `content` — `MessageKeyEntry.content.key`

- headline: Release hold action
- instanceId: `MSGKE-MKEY-9`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-9/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
screen.order.action.release
```

### 27. `content` — `MessageKeyEntry.content.defaultCopy`

- headline: Release hold action
- instanceId: `MSGKE-MKEY-9`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-9/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Release hold
```

### 28. `content` — `MessageKeyEntry.content.description`

- headline: Release hold action
- instanceId: `MSGKE-MKEY-9`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-9/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Header action that releases a credit or stock hold.
```

### 29. `content` — `MessageKeyEntry.content.key`

- headline: Amendment rejected message
- instanceId: `MSGKE-MKEY-10`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-10/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
screen.order.amend.error
```

### 30. `content` — `MessageKeyEntry.content.defaultCopy`

- headline: Amendment rejected message
- instanceId: `MSGKE-MKEY-10`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-10/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
The amendment could not be applied
```

### 31. `content` — `MessageKeyEntry.content.description`

- headline: Amendment rejected message
- instanceId: `MSGKE-MKEY-10`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-10/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Shown on order detail when a line amendment fails validation or reservation.
```

### 32. `content` — `MessageKeyEntry.content.key`

- headline: Retry amendment action
- instanceId: `MSGKE-MKEY-11`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-11/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
screen.order.amend.action.retry
```

### 33. `content` — `MessageKeyEntry.content.defaultCopy`

- headline: Retry amendment action
- instanceId: `MSGKE-MKEY-11`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-11/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Retry
```

### 34. `content` — `MessageKeyEntry.content.description`

- headline: Retry amendment action
- instanceId: `MSGKE-MKEY-11`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-11/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Recovery action offered after a rejected amendment.
```

### 35. `content` — `MessageKeyEntry.content.key`

- headline: Version conflict message
- instanceId: `MSGKE-MKEY-12`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-12/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
error.order.versionConflict
```

### 36. `content` — `MessageKeyEntry.content.defaultCopy`

- headline: Version conflict message
- instanceId: `MSGKE-MKEY-12`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-12/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
The order changed while you were editing; reload and retry
```

### 37. `content` — `MessageKeyEntry.content.description`

- headline: Version conflict message
- instanceId: `MSGKE-MKEY-12`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-12/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Copy of the ORDER_VERSION_CONFLICT error code.
```

### 38. `content` — `MessageKeyEntry.content.key`

- headline: Amend line operation description
- instanceId: `MSGKE-MKEY-13`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-13/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
op.order.amendLine
```

### 39. `content` — `MessageKeyEntry.content.defaultCopy`

- headline: Amend line operation description
- instanceId: `MSGKE-MKEY-13`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-13/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Re-prices and re-reserves an amended order line
```

### 40. `content` — `MessageKeyEntry.content.description`

- headline: Amend line operation description
- instanceId: `MSGKE-MKEY-13`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-13/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Description of the amendOrderLine server operation.
```

### 41. `content` — `MessageKeyEntry.content.key`

- headline: Work list operation description
- instanceId: `MSGKE-MKEY-14`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-14/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
op.order.fetchWorkList
```

### 42. `content` — `MessageKeyEntry.content.defaultCopy`

- headline: Work list operation description
- instanceId: `MSGKE-MKEY-14`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-14/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Fetches a state-filtered page of the order work list
```

### 43. `content` — `MessageKeyEntry.content.description`

- headline: Work list operation description
- instanceId: `MSGKE-MKEY-14`
- path: `SBP/informationAndDataModel/messageKeyRegistry/MSGKE-MKEY-LST-14/content`
- routed by: `CodeSpecPart.text` declared on `MessageKeyEntry`
- routing note: CE-TX — the author-once copy string every consumer references. The key is the join token: CE-EL/CE-AC labels, domainEnum value copy (DomainEnumValueEntry.copyKey), CE-ER error copy (ErrorCodeEntry.copyKey) and CE-VA messages all resolve here. The default copy plus locale variants become the generated message catalogue.

``` text
Description of the fetchOrderWorkList server operation.
```

