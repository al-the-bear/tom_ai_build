# C++ build artifacts — regenerated by `make` / `run_tests.sh`.
# Only the generated header/source (include/, src/), the Makefile, meta/,
# schemas/, the behavioural test (tests/), the hand-authored examples/, the test
# runner and the READMEs are tracked.
build/
*.o
*.a

# --- tom_specs packaging build artifacts (managed) ---
*.so
*.so.*
*.pc
*.tar.gz
