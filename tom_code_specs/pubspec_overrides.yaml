# Local-development override (same mechanism as tom_som_dart_v0's generated
# overrides file): resolve the co-developed release-set sibling by path. This
# file is excluded from `dart pub publish`; the published package uses the
# hosted constraint declared in pubspec.yaml.
dependency_overrides:
  tom_specs_core:
    path: ../tom_specs_core
