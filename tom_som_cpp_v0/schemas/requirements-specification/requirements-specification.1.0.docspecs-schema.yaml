# Generated from the TomSpecs object model — do not edit.
# Schema: requirements-specification/1.0

title-format: "# <!--[RSP]--> Requirements Specification"
section-types:
  daenre-enti:
    prefix: DAENRE_ENTI_
    pattern-check-id:
      pattern: "^DAENRE-ENTI-[0-9]+$"
      error-message: IDs of this section must match DAENRE-ENTI-xxx
    description: "Add one entry per data entity this requirement reads, creates, updates, or deletes."
  rqbiru-rule:
    prefix: RQBIRU_RULE_
    pattern-check-id:
      pattern: "^RQBIRU-RULE-[0-9]+$"
      error-message: IDs of this section must match RQBIRU-RULE-xxx
    description: Add one entry per business rule that constrains or guides this requirement.
  rqscac-acti:
    prefix: RQSCAC_ACTI_
    subsection-types:
      acpr-para:
        max-count: infinite
    pattern-check-id:
      pattern: "^RQSCAC-ACTI-[0-9]+$"
      error-message: IDs of this section must match RQSCAC-ACTI-xxx
    description: Define actions available in the UI.
  fldvl-vali:
    prefix: FLDVL_VALI_
    pattern-check-id:
      pattern: "^FLDVL-VALI-[0-9]+$"
      error-message: IDs of this section must match FLDVL-VALI-xxx
    description: Add one entry per validation rule applied to this field.
  orgim-acti:
    prefix: ORGIM_ACTI_
    pattern-check-id:
      pattern: "^ORGIM-ACTI-[0-9]+$"
      error-message: IDs of this section must match ORGIM-ACTI-xxx
    description: Add one entry per implementation activity for this organizational change.
  rqdep-item:
    prefix: RQDEP_ITEM_
    pattern-check-id:
      pattern: "^RQDEP-ITEM-[0-9]+$"
      error-message: IDs of this section must match RQDEP-ITEM-xxx
    description: Add one entry per dependency on another requirement.
  rqtsc-test:
    prefix: RQTSC_TEST_
    subsection-types:
      rtcee:
        max-count: 1
      rtcea:
        max-count: 1
    pattern-check-id:
      pattern: "^RQTSC-TEST-[0-9]+$"
      error-message: IDs of this section must match RQTSC-TEST-xxx
    description: Add one entry per test case that verifies this requirement.
  scbhv-beha:
    prefix: SCBHV_BEHA_
    pattern-check-id:
      pattern: "^SCBHV-BEHA-[0-9]+$"
      error-message: IDs of this section must match SCBHV-BEHA-xxx
    description: Define dynamic behaviors and interactions.
  scfld-fiel:
    prefix: SCFLD_FIEL_
    subsection-types:
      sfdb:
        max-count: 1
      scfico:
        max-count: 1
      scfiva:
        max-count: 1
      scfila:
        max-count: 1
      fldvl-vali:
        max-count: infinite
    pattern-check-id:
      pattern: "^SCFLD-FIEL-[0-9]+$"
      error-message: IDs of this section must match SCFLD-FIEL-xxx
    description: Define each field in the UI.
  secct-cont:
    prefix: SECCT_CONT_
    subsection-types:
      scei:
        max-count: 1
      scev:
        max-count: 1
    pattern-check-id:
      pattern: "^SECCT-CONT-[0-9]+$"
      error-message: IDs of this section must match SECCT-CONT-xxx
    description: Add one entry per security control implementing this requirement.
  secrq-requ:
    prefix: SECRQ_REQU_
    subsection-types:
      sereencl:
        max-count: 1
      sereenco:
        max-count: 1
      srev:
        max-count: 1
      sereenst:
        max-count: 1
      rac:
        max-count: 1
      seco:
        max-count: 1
      reqdep:
        max-count: 1
      rt:
        max-count: 1
    pattern-check-id:
      pattern: "^SECRQ-REQU-[0-9]+$"
      error-message: IDs of this section must match SECRQ-REQU-xxx
    description: Add one entry per security requirement.
  accr-crit:
    prefix: ACCR_CRIT_
    pattern-check-id:
      pattern: "^ACCR-CRIT-[0-9]+$"
      error-message: IDs of this section must match ACCR-CRIT-xxx
    description: Add one criterion per testable condition.
  acpr-para:
    prefix: ACPR_PARA_
    pattern-check-id:
      pattern: "^ACPR-PARA-[0-9]+$"
      error-message: IDs of this section must match ACPR-PARA-xxx
    description: Add one entry per parameter passed to this action.
  orrq-requ:
    prefix: ORRQ_REQU_
    subsection-types:
      orec:
        max-count: 1
      orei:
        max-count: 1
      orep:
        max-count: 1
      rac:
        max-count: 1
      orip:
        max-count: 1
      reqdep:
        max-count: 1
    pattern-check-id:
      pattern: "^ORRQ-REQU-[0-9]+$"
      error-message: IDs of this section must match ORRQ-REQU-xxx
    description: Add one entry per organizational requirement.
  rere-requ:
    prefix: RERE_REQU_
    pattern-check-id:
      pattern: "^RERE-REQU-[0-9]+$"
      error-message: IDs of this section must match RERE-REQU-xxx
    description: 4.3.5. Requirement Relationships.
  terq-requ:
    prefix: TERQ_REQU_
    subsection-types:
      tred:
        max-count: 1
      trem:
        max-count: 1
      trev:
        max-count: 1
      trei:
        max-count: 1
      trec:
        max-count: 1
      rac:
        max-count: 1
      reqdep:
        max-count: 1
      rt:
        max-count: 1
    pattern-check-id:
      pattern: "^TERQ-REQU-[0-9]+$"
      error-message: IDs of this section must match TERQ-REQU-xxx
    description: Add one entry per technical requirement.
  fre-requ:
    prefix: FRE_REQU_
    subsection-types:
      fred:
        max-count: 1
      frep:
        max-count: 1
      fres:
        max-count: 1
      frev:
        max-count: 1
      frec:
        max-count: 1
      frem:
        max-count: 1
      rac:
        max-count: 1
      rbr:
        max-count: 1
      rdr:
        max-count: 1
      rus:
        max-count: 1
      reqdep:
        max-count: 1
      rt:
        max-count: 1
      reteca:
        max-count: 1
    pattern-check-id:
      pattern: "^FRE-REQU-[0-9]+$"
      error-message: IDs of this section must match FRE-REQU-xxx
    description: "Add one entry per functional requirement. Group related requirements together. Each requirement should be atomic, testable, and have clear acceptance criteria."
  sereencl:
    prefix: SEREENCL
    description: Category and classification.
  sereenco:
    prefix: SEREENCO
    description: Compliance framework mapping.
  sereenst:
    prefix: SEREENST
    description: Status and ownership.
  reqcov:
    prefix: REQCOV
    description: Requirement coverage.
  reqdep:
    prefix: REQDEP
    subsection-types:
      rqdep-item:
        max-count: infinite
    description: Identify requirements that must be implemented before or alongside this requirement.
  reteca:
    prefix: RETECA
    subsection-types:
      rqtsc-test:
        max-count: infinite
    description: Define test cases that verify requirement implementation.
  retrar:
    prefix: RETRAR
    description: Linked artifacts and test coverage references.
  retrim:
    prefix: RETRIM
    description: Implementation and deployment tracking.
  scfico:
    prefix: SCFICO
    description: Conditional behavior.
  scfila:
    prefix: SCFILA
    description: UI and layout.
  scfiva:
    prefix: SCFIVA
    description: Validation rules.
  dochd:
    prefix: DOCHD
    description: Standard TomSpecs document header.
  rtcea:
    prefix: RTCEA
    description: Automation and prioritization details.
  rtcee:
    prefix: RTCEE
    description: Test execution details.
  frec:
    prefix: FREC
    description: Assumptions and constraints.
  fred:
    prefix: FRED
    description: "Requirement details: description, type, category."
  frem:
    prefix: FREM
    description: Version metadata.
  frep:
    prefix: FREP
    description: Priority and effort assessment.
  fres:
    prefix: FRES
    description: Source and rationale.
  frev:
    prefix: FREV
    description: Verification criteria.
  orec:
    prefix: OREC
    description: Requirement classification and source.
  orei:
    prefix: OREI
    description: Impact and change profile.
  orep:
    prefix: OREP
    description: "Planning, ownership, and success tracking."
  orip:
    prefix: ORIP
    subsection-types:
      orgim-acti:
        max-count: infinite
    description: Define the implementation approach for this organizational change requirement.
  scei:
    prefix: SCEI
    description: Control implementation details.
  scev:
    prefix: SCEV
    description: Testing and lifecycle status.
  seco:
    prefix: SECO
    subsection-types:
      secct-cont:
        max-count: infinite
    description: Define security controls that implement this requirement.
  sfdb:
    prefix: SFDB
    description: Data binding and defaults.
  srev:
    prefix: SREV
    description: Implementation and verification.
  trec:
    prefix: TREC
    description: Assumptions and constraints.
  tred:
    prefix: TRED
    description: "Requirement details: description, category, priority."
  trei:
    prefix: TREI
    description: Impact assessment.
  trem:
    prefix: TREM
    description: Measurement specifications.
  trev:
    prefix: TREV
    description: Verification approach and tools.
  rac:
    prefix: RAC
    subsection-types:
      accr-crit:
        max-count: infinite
    description: "Define clear, testable acceptance criteria. Use Given-When-Then format: Given [context], When [action], Then [expected result]."
  rbr:
    prefix: RBR
    subsection-types:
      rqbiru-rule:
        max-count: infinite
    description: "Define business rules that affect this requirement. Business rules are constraints, calculations, or policies from the business domain."
  rdr:
    prefix: RDR
    subsection-types:
      daenre-enti:
        max-count: infinite
    description: "Define the data entities and attributes this requirement reads, creates, updates, or deletes."
  rus:
    prefix: RUS
    subsection-types:
      scfld-fiel:
        max-count: infinite
      rqscac-acti:
        max-count: infinite
      scbhv-beha:
        max-count: infinite
    description: "Define the UI elements needed to support this requirement. Specify screens, forms, fields, actions, and behaviors."
  sr1:
    prefix: SR1
    subsection-types:
      secrq-requ:
        max-count: infinite
    description: "Security requirements protect confidentiality, integrity, and availability of information. Include authentication, authorization, data protection, and compliance requirements."
  tr1:
    prefix: TR1
    subsection-types:
      terq-requ:
        max-count: infinite
    description: "Technical requirements describe non-functional aspects and constraints. Each should be measurable and testable. Common categories: Performance, Scalability, Availability, Security, Maintainability."
  fr:
    prefix: FR
    subsection-types:
      fre-requ:
        max-count: infinite
        min-count: 1
    description: "Functional requirements describe system capabilities, behaviors, and features. Use clear, testable language. Each requirement should answer: What must the system do? For whom? Under what conditions?"
  or:
    prefix: OR
    subsection-types:
      orrq-requ:
        max-count: infinite
    description: "Organizational requirements describe non-technical changes needed for system success: training, process changes, role changes, support structures, and communication."
  rt:
    prefix: RT
    subsection-types:
      retrar:
        max-count: 1
      retrim:
        max-count: 1
    description: Document traceability links to maintain visibility of requirements throughout the project lifecycle.
document:
  sections:
    dochd:
      section-type: dochd
      optional: true
    fr:
      section-type: fr
      optional: true
    tr1:
      section-type: tr1
      optional: true
    sr1:
      section-type: sr1
      optional: true
    or:
      section-type: or
      optional: true
    rere-requ:
      section-type: rere-requ
      optional: true
    reqcov:
      section-type: reqcov
      optional: true
