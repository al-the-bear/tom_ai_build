# Generated from the TomSpecs object model — do not edit.
# Schema: information-model/1.0

title-format: "# <!--[IFM]--> Information Model"
section-types:
  biobat-attr:
    prefix: BIOBAT_ATTR_
    subsection-types:
      boaed:
        max-count: 1
      boaev:
        max-count: 1
      boaeg:
        max-count: 1
    pattern-check-id:
      pattern: "^BIOBAT-ATTR-[0-9]+$"
      error-message: IDs of this section must match BIOBAT-ATTR-xxx
    description: Add one entry per business object attribute.
  birure-keyb:
    prefix: BIRURE_KEYB_
    pattern-check-id:
      pattern: "^BIRURE-KEYB-[0-9]+$"
      error-message: IDs of this section must match BIRURE-KEYB-xxx
    description: Add one entry per business rule reference.
  rulexm-exam:
    prefix: RULEXM_EXAM_
    pattern-check-id:
      pattern: "^RULEXM-EXAM-[0-9]+$"
      error-message: IDs of this section must match RULEXM-EXAM-xxx
    description: Add one entry per rule example.
  acrse-acce:
    prefix: ACRSE_ACCE_
    pattern-check-id:
      pattern: "^ACRSE-ACCE-[0-9]+$"
      error-message: IDs of this section must match ACRSE-ACCE-xxx
    description: Add one entry per access restriction.
  behav-beha:
    prefix: BEHAV_BEHA_
    pattern-check-id:
      pattern: "^BEHAV-BEHA-[0-9]+$"
      error-message: IDs of this section must match BEHAV-BEHA-xxx
    description: Add one entry per behavior rule.
  bjoen-obje:
    prefix: BJOEN_OBJE_
    subsection-types:
      behav-beha:
        max-count: infinite
      integ-inte:
        max-count: infinite
      biobat-attr:
        max-count: infinite
      obst-keys:
        max-count: infinite
      birure-keyb:
        max-count: infinite
      lftrs-life:
        max-count: infinite
      obop-oper:
        max-count: infinite
      obinv-inva:
        max-count: infinite
    pattern-check-id:
      pattern: "^BJOEN-OBJE-[0-9]+$"
      error-message: IDs of this section must match BJOEN-OBJE-xxx
    description: A business object entry (form).
  daatt-attr:
    prefix: DAATT_ATTR_
    subsection-types:
      dataa-cons:
        max-count: infinite
      displ-disp:
        max-count: infinite
    pattern-check-id:
      pattern: "^DAATT-ATTR-[0-9]+$"
      error-message: IDs of this section must match DAATT-ATTR-xxx
    description: Add one entry per data attribute.
  daent-enti:
    prefix: DAENT_ENTI_
    subsection-types:
      volum-volu:
        max-count: infinite
      cre-comp:
        max-count: infinite
      techn-tech:
        max-count: infinite
      daatt-attr:
        max-count: infinite
      keatt-keya:
        max-count: infinite
      enidx-inde:
        max-count: infinite
      encns-cons:
        max-count: infinite
      migme-migr:
        max-count: infinite
    pattern-check-id:
      pattern: "^DAENT-ENTI-[0-9]+$"
      error-message: IDs of this section must match DAENT-ENTI-xxx
    description: A data entity entry (form).
  dataa-cons:
    prefix: DATAA_CONS_
    pattern-check-id:
      pattern: "^DATAA-CONS-[0-9]+$"
      error-message: IDs of this section must match DATAA-CONS-xxx
    description: Add one entry per attribute constraint.
  dclse-item:
    prefix: DCLSE_ITEM_
    subsection-types:
      hndre-hand:
        max-count: infinite
      acrse-acce:
        max-count: infinite
    pattern-check-id:
      pattern: "^DCLSE-ITEM-[0-9]+$"
      error-message: IDs of this section must match DCLSE-ITEM-xxx
    description: Add one entry per data classification level.
  displ-disp:
    prefix: DISPL_DISP_
    pattern-check-id:
      pattern: "^DISPL-DISP-[0-9]+$"
      error-message: IDs of this section must match DISPL-DISP-xxx
    description: Add one entry per display property.
  encns-cons:
    prefix: ENCNS_CONS_
    pattern-check-id:
      pattern: "^ENCNS-CONS-[0-9]+$"
      error-message: IDs of this section must match ENCNS-CONS-xxx
    description: Add one entry per entity constraint.
  enidx-inde:
    prefix: ENIDX_INDE_
    pattern-check-id:
      pattern: "^ENIDX-INDE-[0-9]+$"
      error-message: IDs of this section must match ENIDX-INDE-xxx
    description: Add one entry per entity index.
  enrle-item:
    prefix: ENRLE_ITEM_
    subsection-types:
      parti-part:
        max-count: infinite
      relat-rela:
        max-count: infinite
    pattern-check-id:
      pattern: "^ENRLE-ITEM-[0-9]+$"
      error-message: IDs of this section must match ENRLE-ITEM-xxx
    description: Add one entry per entity relationship.
  fndmx-func:
    prefix: FNDMX_FUNC_
    pattern-check-id:
      pattern: "^FNDMX-FUNC-[0-9]+$"
      error-message: IDs of this section must match FNDMX-FUNC-xxx
    description: A function-to-data matrix entry (form).
  funct-func:
    prefix: FUNCT_FUNC_
    subsection-types:
      fuencl:
        max-count: 1
      fuenop:
        max-count: 1
      fuenim:
        max-count: 1
      sufn-subf:
        max-count: infinite
    pattern-check-id:
      pattern: "^FUNCT-FUNC-[0-9]+$"
      error-message: IDs of this section must match FUNCT-FUNC-xxx
    description: A function entry (form).
  hndre-hand:
    prefix: HNDRE_HAND_
    pattern-check-id:
      pattern: "^HNDRE-HAND-[0-9]+$"
      error-message: IDs of this section must match HNDRE-HAND-xxx
    description: Add one entry per handling requirement.
  integ-inte:
    prefix: INTEG_INTE_
    pattern-check-id:
      pattern: "^INTEG-INTE-[0-9]+$"
      error-message: IDs of this section must match INTEG-INTE-xxx
    description: Add one entry per integration point.
  keatt-keya:
    prefix: KEATT_KEYA_
    subsection-types:
      keagn:
        max-count: 1
      kearf:
        max-count: 1
      keagv:
        max-count: 1
    pattern-check-id:
      pattern: "^KEATT-KEYA-[0-9]+$"
      error-message: IDs of this section must match KEATT-KEYA-xxx
    description: Add one entry per key attribute.
  lftrs-life:
    prefix: LFTRS_LIFE_
    subsection-types:
      ltet:
        max-count: 1
      ltec:
        max-count: 1
      ltee:
        max-count: 1
    pattern-check-id:
      pattern: "^LFTRS-LIFE-[0-9]+$"
      error-message: IDs of this section must match LFTRS-LIFE-xxx
    description: Add one entry per lifecycle transition.
  migme-migr:
    prefix: MIGME_MIGR_
    pattern-check-id:
      pattern: "^MIGME-MIGR-[0-9]+$"
      error-message: IDs of this section must match MIGME-MIGR-xxx
    description: Add one entry per migration mapping.
  obinv-inva:
    prefix: OBINV_INVA_
    pattern-check-id:
      pattern: "^OBINV-INVA-[0-9]+$"
      error-message: IDs of this section must match OBINV-INVA-xxx
    description: Add one entry per object invariant.
  parti-part:
    prefix: PARTI_PART_
    pattern-check-id:
      pattern: "^PARTI-PART-[0-9]+$"
      error-message: IDs of this section must match PARTI-PART-xxx
    description: Add one entry per participating entity.
  relat-rela:
    prefix: RELAT_RELA_
    pattern-check-id:
      pattern: "^RELAT-RELA-[0-9]+$"
      error-message: IDs of this section must match RELAT-RELA-xxx
    description: Add one entry per relationship attribute.
  techn-tech:
    prefix: TECHN_TECH_
    pattern-check-id:
      pattern: "^TECHN-TECH-[0-9]+$"
      error-message: IDs of this section must match TECHN-TECH-xxx
    description: Add one entry per technical characteristic.
  volum-volu:
    prefix: VOLUM_VOLU_
    pattern-check-id:
      pattern: "^VOLUM-VOLU-[0-9]+$"
      error-message: IDs of this section must match VOLUM-VOLU-xxx
    description: Add one entry per volume metric.
  affn-affe:
    prefix: AFFN_AFFE_
    pattern-check-id:
      pattern: "^AFFN-AFFE-[0-9]+$"
      error-message: IDs of this section must match AFFN-AFFE-xxx
    description: Add one entry per affected function.
  afob-affe:
    prefix: AFOB_AFFE_
    pattern-check-id:
      pattern: "^AFOB-AFFE-[0-9]+$"
      error-message: IDs of this section must match AFOB-AFFE-xxx
    description: Add one entry per affected object.
  biru-busi:
    prefix: BIRU_BUSI_
    subsection-types:
      afob-affe:
        max-count: infinite
      affn-affe:
        max-count: infinite
      rulexm-exam:
        max-count: infinite
    pattern-check-id:
      pattern: "^BIRU-BUSI-[0-9]+$"
      error-message: IDs of this section must match BIRU-BUSI-xxx
    description: A business rule entry (form).
  obop-oper:
    prefix: OBOP_OPER_
    subsection-types:
      ooee:
        max-count: 1
      ooel:
        max-count: 1
      ooeg:
        max-count: 1
    pattern-check-id:
      pattern: "^OBOP-OPER-[0-9]+$"
      error-message: IDs of this section must match OBOP-OPER-xxx
    description: Add one entry per object operation.
  obst-keys:
    prefix: OBST_KEYS_
    pattern-check-id:
      pattern: "^OBST-KEYS-[0-9]+$"
      error-message: IDs of this section must match OBST-KEYS-xxx
    description: Add one entry per object state.
  sufn-subf:
    prefix: SUFN_SUBF_
    pattern-check-id:
      pattern: "^SUFN-SUBF-[0-9]+$"
      error-message: IDs of this section must match SUFN-SUBF-xxx
    description: Add one entry per sub-function.
  cre-comp:
    prefix: CRE_COMP_
    pattern-check-id:
      pattern: "^CRE-COMP-[0-9]+$"
      error-message: IDs of this section must match CRE-COMP-xxx
    description: Add one entry per compliance requirement.
  fuencl:
    prefix: FUENCL
    description: Decomposition position and classification.
  fuenim:
    prefix: FUENIM
    description: Automation and data handling summary.
  fuenop:
    prefix: FUENOP
    description: Execution profile and criticality.
  boaed:
    prefix: BOAED
    description: Format and requirement details.
  boaeg:
    prefix: BOAEG
    description: Sensitivity and presentation guidance.
  boaev:
    prefix: BOAEV
    description: Validation and derivation rules.
  datcl:
    prefix: DATCL
    subsection-types:
      dclse-item:
        max-count: infinite
    description: Data classification.
  dochd:
    prefix: DOCHD
    description: Standard TomSpecs document header.
  enrel:
    prefix: ENREL
    subsection-types:
      enrle-item:
        max-count: infinite
    description: Entity relationships.
  keagn:
    prefix: KEAGN
    description: Key generation settings.
  keagv:
    prefix: KEAGV
    description: Constraint semantics and business meaning.
  kearf:
    prefix: KEARF
    description: Foreign-key reference and cascade behavior.
  dadi:
    prefix: DADI
    description: Data dictionary.
  inco:
    prefix: INCO
    description: Integrity constraints.
  ltec:
    prefix: LTEC
    description: Transition conditions and guarantees.
  ltee:
    prefix: LTEE
    description: "Actions, actors, and rollback handling."
  ltet:
    prefix: LTET
    description: Triggering event details.
  ooee:
    prefix: OOEE
    description: Execution contract for this operation.
  ooeg:
    prefix: OOEG
    description: Authorization and usage boundaries.
  ooel:
    prefix: OOEL
    description: State and event lifecycle details.
  vaco:
    prefix: VACO
    description: Validation constraints.
document:
  sections:
    dochd:
      section-type: dochd
      optional: true
    daent-enti:
      section-type: daent-enti
    enrel:
      section-type: enrel
      optional: true
    datcl:
      section-type: datcl
      optional: true
    bjoen-obje:
      section-type: bjoen-obje
    funct-func:
      section-type: funct-func
      optional: true
    fndmx-func:
      section-type: fndmx-func
      optional: true
    biru-busi:
      section-type: biru-busi
    dadi:
      section-type: dadi
      optional: true
    vaco:
      section-type: vaco
      optional: true
    inco:
      section-type: inco
      optional: true
