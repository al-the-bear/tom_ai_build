# TomSpecs Core API Reference: Index

The public API of `tom_specs_core`, one summary per module. Both modules are
exported from the single barrel `package:tom_specs_core/tom_specs_core.dart`.

| Module | Summary | Covers |
|--------|---------|--------|
| `annotations` | [api_summary_annotations.md](api_summary_annotations.md) | The 34 annotation classes, the `Field` element of `@Form`, and the 3 closed enums |
| `sections` | [api_summary_sections.md](api_summary_sections.md) | `DocSpecsSection`, `DocSpecsForm`, and the 10 content-typed section leaves |

For task-oriented guides rather than reference tables, see the
[documentation index](../index.md).
