// GENERATED by tom_specs_clitool SomGoEmitter (v0) — do not edit by hand.
//
// Typed object-model facade over the generic `tom_som_go_runtime` document.
//
// The generic runtime is imported through a fixed import path; the generator wires
// resolution by writing a `go.mod` with a relative `replace` directive, so this
// module carries no on-disk path of its own.
package somv0

import som "tom_som_go_runtime"

// Generated enum constants for `Probability` values. The stored token is byte-
// identical across every language port, so documents stay cross-compatible.
const (
	ProbabilityLow = "low"
	ProbabilityMedium = "medium"
	ProbabilityHigh = "high"
)

// parseProbability returns token when it is a known Probability value, else "".
func parseProbability(token string) string {
	switch token {
	case ProbabilityLow, ProbabilityMedium, ProbabilityHigh:
		return token
	}
	return ""
}

type CurrentSituation struct {
	som.SomNode
}

// NewCurrentSituation binds a CurrentSituation facade to a document and a path.
func NewCurrentSituation(doc *som.SpecDocument, path string) *CurrentSituation {
	return &CurrentSituation{SomNode: som.NewSomNode(doc, path)}
}

func (x *CurrentSituation) Summary() string {
	return x.Doc().ContentOr(x.Path() + "/summary")
}

func (x *CurrentSituation) SetSummary(value string) {
	x.Doc().SetContent(x.Path() + "/summary", value)
}

// Root of a project definition document.
type ProjectDefinition struct {
	som.SomNode
}

// ProjectDefinitionModelVersion is the model version this object model was generated
// against (§2.1).
const ProjectDefinitionModelVersion = "0.0"

// NewProjectDefinition creates the typed facade at the document root and verifies the
// document's authoring documentVersion is editable (§2.2). A non-editable
// stamp yields a *som.SomVersionError.
func NewProjectDefinition(doc *som.SpecDocument, documentVersion string) (*ProjectDefinition, error) {
	if err := som.CheckSomModelVersion(ProjectDefinitionModelVersion, documentVersion); err != nil {
		return nil, err
	}
	return &ProjectDefinition{SomNode: som.NewSomNode(doc, "PD00")}, nil
}

// ObjectModelVersion returns this object model's own model version (major.minor),
// per §2.1.
func (x *ProjectDefinition) ObjectModelVersion() string {
	return ProjectDefinitionModelVersion
}

// Why the system exists.
func (x *ProjectDefinition) Vision() string {
	return x.Doc().ContentOr(x.Path() + "/vision")
}

func (x *ProjectDefinition) SetVision(value string) {
	x.Doc().SetContent(x.Path() + "/vision", value)
}

func (x *ProjectDefinition) Owner() *ProjectDefinitionOwnerForm {
	return NewProjectDefinitionOwnerForm(x.Doc(), x.Path() + "/owner")
}

func (x *ProjectDefinition) Risks() *som.SomList[*Risk] {
	return som.NewSomList(x.Doc(), x.Path() + "/risks", func(d *som.SpecDocument, p string) *Risk {
		return NewRisk(d, p)
	})
}

func (x *ProjectDefinition) Tags() *som.SomList[*som.SomScalar] {
	return som.NewSomList(x.Doc(), x.Path() + "/tags", func(d *som.SpecDocument, p string) *som.SomScalar {
		return som.NewSomScalar(d, p)
	})
}

func (x *ProjectDefinition) Situation() *CurrentSituation {
	return NewCurrentSituation(x.Doc(), x.Path() + "/situation")
}

type Risk struct {
	som.SomNode
}

// NewRisk binds a Risk facade to a document and a path.
func NewRisk(doc *som.SpecDocument, path string) *Risk {
	return &Risk{SomNode: som.NewSomNode(doc, path)}
}

func (x *Risk) Title() string {
	return x.Doc().ContentOr(x.Path() + "/title")
}

func (x *Risk) SetTitle(value string) {
	x.Doc().SetContent(x.Path() + "/title", value)
}

func (x *Risk) Probability() string {
	return parseProbability(x.Doc().ContentOr(x.Path() + "/prob"))
}

func (x *Risk) SetProbability(value string) {
	x.Doc().SetContent(x.Path() + "/prob", value)
}

// ProjectDefinitionOwnerForm is the generated form facade for the `owner` @Form section.
type ProjectDefinitionOwnerForm struct {
	som.SomNode
}

// NewProjectDefinitionOwnerForm binds a ProjectDefinitionOwnerForm facade to a document and a path.
func NewProjectDefinitionOwnerForm(doc *som.SpecDocument, path string) *ProjectDefinitionOwnerForm {
	return &ProjectDefinitionOwnerForm{SomNode: som.NewSomNode(doc, path)}
}

func (x *ProjectDefinitionOwnerForm) Name() string {
	return x.Doc().FormFieldOr(x.Path(), "name")
}

func (x *ProjectDefinitionOwnerForm) SetName(value string) {
	x.Doc().SetFormField(x.Path(), "name", value)
}

func (x *ProjectDefinitionOwnerForm) Role() string {
	return x.Doc().FormFieldOr(x.Path(), "role")
}

func (x *ProjectDefinitionOwnerForm) SetRole(value string) {
	x.Doc().SetFormField(x.Path(), "role", value)
}

