# Java build artifacts — regenerated by `javac` / `run_tests.sh` / the golden
# harness. Only the generated module (src/), meta/, schemas/, the behavioural
# test (tests/), the hand-authored examples/, the golden generator (tool/), the
# test runner and the READMEs are tracked.
build/
build_tool/
*.class
