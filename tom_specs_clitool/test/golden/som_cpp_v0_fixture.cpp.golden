// GENERATED by tom_specs_clitool SomCppEmitter (v0) — do not edit by hand.
// Typed object-model facade (source) over the generic tom_som_cpp_runtime document.

#include "tom_som_cpp_v0.hpp"

#include <optional>
#include <utility>

#include "tom_som_cpp_v0_meta.hpp"

namespace tom_som_v0 {

std::optional<std::string> Probability::parse(const std::string& token) {
  if (token == low) {
    return token;
  }
  if (token == medium) {
    return token;
  }
  if (token == high) {
    return token;
  }
  return std::nullopt;
}

CurrentLandscapeAssessment::CurrentLandscapeAssessment(som::SpecDocument& doc, std::string path)
    : som::SomNode(doc, std::move(path)) {}
std::string CurrentLandscapeAssessment::summary() const {
  return doc().content(som::joinPath(path(), "summary"));
}
void CurrentLandscapeAssessment::setSummary(const std::string& value) {
  doc().setContent(som::joinPath(path(), "summary"), value);
}

Risk::Risk(som::SpecDocument& doc, std::string path)
    : som::SomNode(doc, std::move(path)) {}
std::string Risk::title() const {
  return doc().content(som::joinPath(path(), "title"));
}
void Risk::setTitle(const std::string& value) {
  doc().setContent(som::joinPath(path(), "title"), value);
}
std::optional<std::string> Risk::probability() const {
  return Probability::parse(doc().content(som::joinPath(path(), "prob")));
}
void Risk::setProbability(const std::string& value) {
  doc().setContent(som::joinPath(path(), "prob"), value);
}

SolutionBlueprint::SolutionBlueprint(som::SpecDocument& doc, const std::string& documentVersion)
    : som::SomNode(doc, "PD00") {
  som::checkSomModelVersion(kModelVersion, documentVersion);
}
SolutionBlueprint SolutionBlueprint::loadYaml(som::SpecDocument& doc, const std::string& yaml) {
  std::string err;
  std::optional<som::SpecDocument> parsed =
      som::SpecDocument::fromYaml(yaml, tom_som_v0_meta::solutionBlueprintMetaTree(), &err);
  if (!parsed) {
    throw som::SomVersionError(err.empty() ? "loadYaml: decode failed" : err);
  }
  doc = std::move(*parsed);
  return SolutionBlueprint(doc, doc.modelVersion);
}
SolutionBlueprint SolutionBlueprint::loadFile(som::SpecDocument& doc, const std::string& path) {
  std::string err;
  std::optional<som::SpecDocument> parsed =
      som::SpecDocument::fromFile(path, tom_som_v0_meta::solutionBlueprintMetaTree(), &err);
  if (!parsed) {
    throw som::SomVersionError(err.empty() ? "loadFile: decode failed" : err);
  }
  doc = std::move(*parsed);
  return SolutionBlueprint(doc, doc.modelVersion);
}
std::string SolutionBlueprint::objectModelVersion() const {
  return kModelVersion;
}
som::SomEditability SolutionBlueprint::editabilityFor(const std::string& documentVersion) {
  return som::somEditabilityFor(kModelVersion, documentVersion);
}
std::string SolutionBlueprint::vision() const {
  return doc().content(som::joinPath(path(), "vision"));
}
void SolutionBlueprint::setVision(const std::string& value) {
  doc().setContent(som::joinPath(path(), "vision"), value);
}
SolutionBlueprintOwnerForm SolutionBlueprint::owner() const {
  return SolutionBlueprintOwnerForm(doc(), som::joinPath(path(), "owner"));
}
som::SomList SolutionBlueprint::risks() const {
  return som::SomList(doc(), som::joinPath(path(), "risks"), "RISK-ITEM-xxx");
}
som::SomList SolutionBlueprint::tags() const {
  return som::SomList(doc(), som::joinPath(path(), "tags"), "");
}
CurrentLandscapeAssessment SolutionBlueprint::situation() const {
  return CurrentLandscapeAssessment(doc(), som::joinPath(path(), "situation"));
}

SolutionBlueprintOwnerForm::SolutionBlueprintOwnerForm(som::SpecDocument& doc, std::string path)
    : som::SomNode(doc, std::move(path)) {}
std::string SolutionBlueprintOwnerForm::name() const {
  return doc().formField(path(), "name");
}
void SolutionBlueprintOwnerForm::setName(const std::string& value) {
  doc().setFormField(path(), "name", value);
}
std::string SolutionBlueprintOwnerForm::role() const {
  return doc().formField(path(), "role");
}
void SolutionBlueprintOwnerForm::setRole(const std::string& value) {
  doc().setFormField(path(), "role", value);
}

}  // namespace tom_som_v0
