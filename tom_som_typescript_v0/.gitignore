# TypeScript build output and installed dependencies — regenerated by
# `npm install` + `npm run build` (the runtime is linked via the relative
# `file:` dependency). Only the source (.ts), meta/, schemas/, package.json
# and tsconfig.json are tracked (package-lock.json is ignored repo-wide).
dist/
node_modules/
