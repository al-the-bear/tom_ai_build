# Tokens that share the quest-todo id shape (>=2 lowercase letters + 1..3
# digits) but are not todo ids.
#
# Read by bin/check_todo_citations.dart. The checker recognises citations by
# *shape* rather than by an enumerated list of series, because an enumeration
# only ever knows the series someone remembered to add — the sweep that
# motivated the checker found eight citations of a series no enumeration
# contained. The price of a shape rule is that ordinary technical terms collide
# with it; this file is that price, paid one word at a time.
#
# Add a token here only when it is genuinely not a todo id. If a token *is* a
# todo id that the corpus cannot resolve, the fix is the citation or the corpus,
# never this file.
#
# One token per line. `#` starts a comment.

address2                      # HTTP/postal address line 2 — an ExperienceDesign form field
arm64                         # CPU architecture
legiondary01                  # a fleet machine name
libsqlite3                    # the SQLite shared library
python3                       # the Python 3 interpreter binary
sqlite3_auto_extension        # SQLite C API
sqlite3_enable_load_extension # SQLite C API
sqlite3_flutter_libs          # a pub.dev package
sqlite3_load_extension        # SQLite C API
sqlite3_vec_init              # the sqlite-vec entry point
vec0                          # the sqlite-vec extension's virtual-table module
csex7                         # a deliberately non-existent id, used in
                              #   check_todo_citations' own prose to show what
                              #   an unknown *series* reads like
csrb99                        # likewise, a wrong *number* in a real series
tier1                         # the lexical retrieval tier (SpecRecall)
tier2                         # the vector retrieval tier (SpecRecall)
utf16_units                   # a tom_som_python_runtime identifier
phase4_codespecs_run          # a tom_specs_samples directory name; `phase4` reads
                              #   as a todo series and the rest as a slug
