# https://dart.dev/guides/libraries/private-files
# Created by `dart pub`
.dart_tool/

# Resolved versions are not committed for path-linked workspace packages
# (siblings in tom_ai/ai_build follow the same convention).
pubspec.lock

# testkit baselines + raw run output (regeneratable from `testkit :test`)
testlog/
