/// Identifying metadata for the `tom_spec_engine` package surface.
library;

/// Human-readable engine name — used in logs and tool banners.
const String tomSpecEngineName = 'tom_spec_engine';

/// Semantic version of the `tom_spec_engine` package surface.
///
/// Kept in step with `pubspec.yaml`'s `version:`.
const String tomSpecEngineVersion = '0.0.1';
