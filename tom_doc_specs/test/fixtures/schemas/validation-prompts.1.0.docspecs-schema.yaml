# Validation prompts for AI validation
section-types:
  requirement:
    prefix: req
    validation-prompt: "Check that each requirement is clear, measurable, and testable"

document:
  sections:
    requirements:
      section-type: requirement
      validation-prompt: "Verify requirements section is complete and consistent"
      subsection-validation-prompt: "Each subsection should describe a single requirement"
